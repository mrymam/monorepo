sqlite:
  filepath: "${SQLITE_FILEPATH}"

jwt:
  secret_key: "${JWT_SECRET_KEY}"

twitter:
  0auth1:
    consumer_key: "${TWITTER_OAUTH1_CONSUMER_KEY}"
    consumer_secret: "${TWITTER_OAUTH1_CONSUMER_SECRET}"
    callback_url: "${TWITTER_OAUTH1_CALLBACK_URL}"
